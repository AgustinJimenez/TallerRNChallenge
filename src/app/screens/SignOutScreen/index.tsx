import React from 'react';
import {SafeAreaView} from 'react-native';
import TopBar from '../../../components/ui/molecules/TopBar';

const SignOutScreen = () => {
  return (
    <SafeAreaView>
      <TopBar>Sign Out</TopBar>
    </SafeAreaView>
  );
};

export default SignOutScreen;
