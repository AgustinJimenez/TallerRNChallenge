import React from 'react';
import {SafeAreaView} from 'react-native';
import SingleTitle from '../../../components/ui/atoms/SingleTitle';

const SignOutScreen = () => {
  return (
    <SafeAreaView>
      <SingleTitle>Sign Out</SingleTitle>
    </SafeAreaView>
  );
};

export default SignOutScreen;
