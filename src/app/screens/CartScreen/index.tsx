import React from 'react';
import {SafeAreaView} from 'react-native';
import SingleTitle from '../../../components/ui/atoms/SingleTitle';

const CartScreen = () => {
  return (
    <SafeAreaView>
      <SingleTitle>CART</SingleTitle>
    </SafeAreaView>
  );
};

export default CartScreen;
